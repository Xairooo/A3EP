[protocol]             ; Protocol configuration
port=2087              ; if you specify just a port number it binds to all ip addresses, if you specify a ip:port then it binds to that ip.

[security]
username = admin    ; Spaces around '=' are stripped
password = default  ; And comments (like this) ignored
